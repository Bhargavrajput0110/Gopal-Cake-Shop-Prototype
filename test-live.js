import {createClient} from '@supabase/supabase-js'; async function test() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); const {data, error} = await supabase.auth.signInWithPassword({email: 'photo@gopalcakeshop.com', password: 'password123'}); if(error) return console.error(error); const projectId = new URL(process.env.NEXT_PUBLIC_SUPABASE_URL).hostname.split('.')[0]; const cookie = \sb-\-auth-token=\\; const res = await fetch('https://gopalcakeshop.com/api/v1/vendor/tasks', {headers: {cookie}}); const json = await res.json(); console.log(JSON.stringify(json, null, 2)); } test();
